apiVersion: v1
kind: Service
metadata:
  name: studentscrud-application
  labels:
    run: studentscrud-application
spec:
  type: NodePort
  ports:
    - port: 8080
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    run: studentscrud-application